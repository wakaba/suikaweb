dependencies:
  override:
    - npm install -g firebase-tools
test:
  override:
    - echo
deployment:
  hub:
    branch: firedata
    commands:
      - echo "{\"firebase\"\: \"$FIREBASE_NAME\", \"public\"\: \"hoge\", \"ignore\"\: [\"**/.*\"]}" > firebase.json
      - mkdir -p hoge
      - echo "foo" > hoge/abc.txt
      - firebase deploy --token $FIREBASE_TOKEN
